# Python 3.13

# ── core ─────────────────────────────────────────────────────────
pygame>=2.6.0,<3.0        # 2.6.0 = first release with Python 3.13 wheels
python-dotenv>=1.0.0

# ── AI modes ─────────────────────────────────────────────────────
openai>=1.0.0

# ── speech modes (text_voice / speech_speech) ────────────────────
# On Pi: sudo apt install libportaudio2 -y  (then pip install sounddevice)
sounddevice>=0.4.6        # uses PortAudio; cleaner than pyaudio for streaming
websockets>=13.0          # 13.x has stable Python 3.13 support
